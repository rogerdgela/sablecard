import styled from "styled-components"

const Logo = styled.div`
    font-size: 70px;
    font-weight: 700;
    letter-spacing: -5px;
    color: black;
    
`

function Logotipo() {
    return(
        <Logo>Sable</Logo>
    )
}

export default Logotipo;