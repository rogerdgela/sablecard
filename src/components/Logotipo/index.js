import styled from "styled-components"

const Logo = styled.span`
    font-size: 40px;
    font-weight: 700;    
`

function Logotipo() {
    return(
        <Logo>Sable.</Logo>
    )
}

export default Logotipo;